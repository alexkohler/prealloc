name: build-test
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
        with:
          go-version: 1.24.x
      - uses: actions/checkout@v6
      - name: Build
        run: go build .
      - name: Test
        run: go test -v .
